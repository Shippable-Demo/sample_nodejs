language: node_js

node_js:
  - 0.12

build:
  pre_ci:
    - docker build -t myCustomImage:latest . 
  pre_ci_boot:
    image_name: myCustomImage
    image_tag: latest
    pull: false
    env: FOO=bar
    options: "-e HOME=/root"
  ci:
    - npm install
